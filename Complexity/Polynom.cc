#include <iostream>

#define N 10

int main(void) {
    for (int i = 0; i < N; i++) {
        for (int j = 0; j < N; j++) {
            for (int k = 0; k < N; k++) {
                // O(n^3)
            }
        }
    }

    return EXIT_SUCCESS;
}